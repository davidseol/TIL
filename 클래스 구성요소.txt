필드
-개발자들마다 다르지만 보통 변수라고 부르는 경우가 많다

메서드
-함수라고 하는데 상태를 바꾸거나 동작을 실행시키는것

void 
-반환하는게 없다 , y 값이 없다
ex) x+1 

생성자
-클래스의 이름하고 같은 이름을 가진다
-이 클래스를 메모리에 생성하는 역할을 한다
-생성자를 안만들면 자바가 기본생성자를 만드는데, 하나라도 있으면 기본생성자를 만들지 않는다.

static
-메소드 영역에 저장된다
-메소드 영역은 나머지 두 영역에서 공유를 할수있다.
-공유를 하기 위해서 static을 붙힌다

String 은 변수가 클래스 / 앞글자가 대문자이기도 하고 그래서 메소드 영역에 생성
int는 데이터  / 힙에 생성 - 힙은 데이터들이 모이는 공간

public - 모든사람접근가능
private - 클래스안에서만 접근 가능
default - 
protected - 